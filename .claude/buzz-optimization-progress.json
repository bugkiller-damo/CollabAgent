{
  "plan": "docs/2026-08-16/02-buzz-vs-slock-optimization-plan.md",
  "startedAt": "2026-08-16",
  "completedItems": ["O5", "O17", "O18", "O1", "O2", "O3", "O4", "O6", "O7", "O8", "O19"],
  "currentItem": "O14",
  "notes": {
    "O5": "config.ts 硬校验：collectInsecureConfig 纯函数（可单测）+ 生产命中默认值 process.exit(1)；ALLOW_INSECURE_DEV_SECRETS=1 仅限本地调试逃生门；DATABASE_URL 默认值移除 P@ssw0rd；新增 packages/server/.env.example + test/config.test.ts（6 通过）",
    "O17": "Biome 2.5.8 全仓接入：biome.json（a11y/格式基线，0 error / 351 warning 不阻断）；5 包 lint 脚本 + 根 lint/lint:fix；ci.yml 分层 4 job（L1 lint+typecheck / L2 daemon tests / L2 web 双构建 / L3 server 真 PG 集成）；lefthook pre-commit 自动修复 + pre-push typecheck；.gitattributes LF 防行尾抖动。子代理修复 58 前端 error + server/daemon bug-class（agent-runtime.ts ANSI 正则改 new RegExp 模板形式，语义等价 82/82 测试过）",
    "O18": "dependabot.yml（npm+gh-actions weekly，minor/patch 分组）+ audit.yml（PR/push/周一定时，--prod --audit-level=high 阻断，moderate 非阻断）",
    "O1": "lib/pubsub.ts（PubSub 抽象：ioredis 双连接 pub/sub + 进程内 EventEmitter 回退；重连重订阅、错误去重日志）；ws/handler.ts 把 broadcast/sendToUser/sendToDaemon/broadcastToDaemons/terminal:frame 改为发布 WsEnvelope 到 slock:ws:v1，各实例订阅后按本地 socket 表投递（私有频道成员过滤保留）；index.ts 注入 + shutdown 关闭。presence（daemonClients.has isOnline）仍为实例本地态，多实例 presence 与 terminalWatchers 共享引用计数列为后续项",
    "O2": "migration 010_events.sql（BIGINT IDENTITY id + 无外键 + 三索引）+ lib/audit.ts（eventHash 稳定序列化 payload 防 jsonb 键序重排；appendEvent 事务内 pg_advisory_xact_lock 串行链头）+ messages.ts send/edit/delete 事务内追加 message.send/edit/delete 事件 + routes/audit.ts（GET /api/audit 对象过滤+分页+message 可见性；GET /api/audit/verify 锚点全局链校验）+ test/audit.test.ts 5 单测。task/channel/approval 事件接入列为增量项。2026-08-16 真 PG 18.4 集成验证：migration 010 实库执行通过、126/126 测试全绿；发现并修复 appendEvent payload 双重编码 bug（JSON.stringify 传字符串→postgres.js 把 jsonb 存成 JSON 字符串值→verify 误报断裂；改为传 JS 对象）；新增 test/audit-api.test.ts 2 测试（send→edit→delete 链校验 valid + 未认证 401）回归固化",
    "O3": "lib/tenant.ts（resolveTenant 优先级：显式 serverId 参数 > x-server-id header > SERVER_HOST_MAP Host 映射 > getDefaultServerId 单租户降级；parseHostMap/resolveHostServerId 纯函数可单测；isServerMember）。显式租户（param/header/host）路由强制成员校验防任意 serverId 枚举；resolveChannel/resolvePeer/resolveDmTarget 支持 serverId 作用域（同名频道/agent 跨社区不串号，idx_channels_server_name/idx_agents_server_name per-server 唯一性生效）。access.ts canAccessChannel 增加 ChannelAccessOptions（serverId 作用域 fail-closed + enforceServerMembership 显式租户成员断言；dm 频道保持双方成员制不套 server 作用域；getChannelInfo 合并缓存查询）。接入路由：channels（GET/POST //resolve）、messages（GET //history/send/search/thread/edits/reactions，search 恒按当前租户 server 过滤——单租户降级也限定默认 server）、tasks（resolveAccessible + from-message）、actions（prepare）、orgs /server/info（显式租户下 humans 仅列社区成员，防跨社区用户枚举）。.env.example 补充 SERVER_HOST_MAP 语义。测试：test/tenant.test.ts 23 个（10 单测 + 13 集成断言：双社区频道列取/发送/读取/搜索/resolve/创建/成员信息隔离 + 非成员 403 + 显式 serverId 参数路径 + 前端回传默认 serverId 建频道的单租户豁免回归）；ws.test 更新（O3 server 级 RBAC 下显式 serverId 建频道需成员前置）；helpers.api() 支持自定义 headers。真 PG 18.4 全量 vitest 149/149（16 文件）通过。兼容豁免：单租户部署（SERVER_HOST_MAP 未配置）下显式声明「默认 server」与不声明降级等价——web/web-vue channelStore 会把 /api/server/info 返回的默认 server id 原样回传建频道，而注册不自动加入默认 server，若强制校验会打坏存量前端建频道；默认社区本就是开放社区（公开频道对全体登录用户可见），豁免不增加暴露面，多租户模式（host 映射已配置）不豁免。遗留增量项：① agent 内部路由（/internal/agent/*）的名字解析尚未接 server 作用域（agent 自身 server 已知，可后续按 getAgent.server_id 限定，注意跨 server 频道成员例外）；② 全局限流的 loginAttempts 内存 Map（O6 范畴）；③ Host 映射端到端测试需 SERVER_HOST_MAP 环境变量（已由单测覆盖解析逻辑，集成测试走 x-server-id 路径）",
    "O4": "主线程接缝（e5f04f9）：lib/storage.ts 保留 Storage 接口（save/read/remove/publicUrl），新增 resolveStorageBackend 纯函数 + sanitizeFilename（去分隔符/控制字符/截断128/「..」回退）+ newStorageKey（uuid/净化文件名）+ 目录穿越 pathFor 加固（base+sep 前缀比较）+ 顶层 await 按 STORAGE_BACKEND 动态 import；config.ts 增加 STORAGE_BACKEND/S3_ENDPOINT/S3_REGION/S3_BUCKET/S3_ACCESS_KEY/S3_SECRET_KEY/S3_FORCE_PATH_STYLE/S3_PUBLIC_BASE_URL + collectInsecureConfig 对 s3/minio 缺参拦截。Graph 双算子并行（implementation worktree 补丁，主线程 git apply 集成）：A=lib/storage-s3.ts S3Storage（@aws-sdk/client-s3@^3.1111.0，S3ClientLike 注入 fake 可单测，构造缺参中文报错，read 404/NoSuchKey 归一 not found，remove 幂等，publicUrl 公共桶直链或 /api/attachments/by-key?key= 代理）+ test/storage-s3.test.ts 15 单测 + test/storage.test.ts 17 单测 + compose minio profile（profiles: s3，本机无 docker 未做真 MinIO E2E，列为增量）；B=routes/attachments.ts 抽 serveAttachment 共用鉴权出口（上传者或频道成员）+ GET /by-key?key=（注册在 /:id 前，支持 ?meta）+ 显式 per-file 上限 413 + newStorageKey 接入；routes/channels.ts 删除链：事务内先收集本频道附件 id → 删 message_attachments 后清孤儿 attachments 行 RETURNING storage_key → 提交后 best-effort getStorage().remove（失败 req.log.warn）+ test/attachments.test.ts 5 集成测试。验证：tsc 0 error、biome 0 error、真 PG 18.4 全量 vitest 186/186（19 文件）。增量项：① 真 MinIO E2E（compose minio profile + env 已备好，docker 环境跑 STORAGE_BACKEND=s3 S3_ENDPOINT=http://localhost:9000 起服后全链路冒烟）；② sharp 缩略图管线（计划中标注可选）；③ 存量本地 /files/ 旧行继续可用（static 路由保留），新上传走 publicUrl 形态",
    "O6": "lib/login-lock.ts（cf94cb5）：LoginLockStore 接口 + MemoryLoginLockStore（固定窗口自首次失败，惰性过期+60s 清扫）+ ValkeyLoginLockStore（INCR+PEXPIRE NX，pttl 剩余锁时，del 清除，Redis 故障 fail-open 与 rate-limit 一致，注入 RedisLike 可单测）；双 key 高维度：login:acct:<小写账号>（阈值 config.LOGIN_MAX_ATTEMPTS=5）+ login:ip:<x-forwarded-for 首个或 req.ip>（阈值新配置 LOGIN_IP_MAX_ATTEMPTS=20，NAT 共享 IP 故显著高于账号维度）；成功登录清双 key；loginLockRemainingMs 取两维度最大值。auth.ts 删除内存 Map/checkLoginLock/recordLoginFail，/login 接入（429 文案保持「登录失败次数过多，请 X 分钟后再试」），recordSession 复用 clientIpOf；.env.example 补 LOGIN_IP_MAX_ATTEMPTS。测试：test/login-lock.test.ts 13 单测（内存阈值/清除/窗口过期、Valkey fake 计数/PEXPIRE/pttl/-2 缺失/故障 fail-open、normalizeAccount/clientIpOf、高层双 key 跨维度锁定与清除）+ auth.test.ts 3 集成测试（同账号跨 3 个 IP 失败 5 次→正确密码 429；4 失败+成功→再 1 失败 401 且再成功 200 证明已清除；幽灵账号 5 失败后 429 防枚举）。真 PG 18.4 全量 vitest 202/202（20 文件）。增量项：① 真 Valkey 多实例共享验证未做（本机无 6379/无 docker；ValkeyLoginLockStore 由 fake 单测覆盖，逻辑与 rate-limit ValkeyBackend 同构）；② IP 维度集成测试刻意未触发（20 次失败会锁死测试机共享 IP 影响全套件），由单测覆盖",
    "O7": "lib/access.ts（3677a05）：TTL 语义固化（ACCESS_CACHE_TTL_MS=2000 导出常量 + 模块头注释：主动失效之外的有界兜底窗口）；新增 invalidateChannel/invalidateMember（精确前缀清缓存）+ setAccessPubSub（订阅 slock:access-inv:v1，远端失效消息清本实例缓存）；失效经 O1 pub/sub 扇出（本地直投 + Redis 远端，宕机错过消息由 TTL 兜底，一致性窗口始终有界）。变更点接入 8 处：channels.ts PATCH /:channelId（类型/可见性）、DELETE /:channelId（频道+全部成员角色）、join/leave/invite/移除成员/角色变更（invalidateMember 精确 userId），dm.ts getOrCreateDmChannel 双方入圈；index.ts setAccessPubSub 注入。测试 test/access-cache.test.ts 10 个：8 单测（fake app 查询计数：缓存命中、invalidate 回源、成员级精确失效、改类型时序「无失效=旧值/失效=立即新值」、TTL 2.2s 过期兜底、pubsub 失效消息与脏数据忽略）+ 2 集成（私有→公开改类型后非成员无 sleep 立即可读；移除成员后被移除者立即 403）。真 PG 18.4 全量 vitest 212/212（21 文件）。增量项：真 Valkey 多实例失效验证未做（与 O1/O6 同，本机无 6379；扇出逻辑复用已验收的 PubSub 抽象）",
    "O8": "metrics.ts 增计数器 machineAuthBcryptScans/Hits；index.ts 与 ws/handler.ts 两处 bcrypt 兼容分支打点（scans 进分支、hits 命中）+ 命中 warn 日志（HTTP request.log.warn 含 userId/scope；WS console.warn）。scripts/audit-bcrypt-tokens.mjs（仓库根，createRequire 锚定 server 包解析 postgres CJS 构建；DATABASE_URL env 或 packages/server/.env；UNION ALL + GROUP BY 序号；按表×哈希×状态统计并直接输出退役判定）+ root package.json audit:bcrypt-tokens script；实库跑通：本机 dev 库 machine_tokens active bcrypt=22（未达退役条件，需轮换）、agent_credentials 全 sha256。退役计划文档 docs/2026-08-16/08-bcrypt-token-retirement.md：两处分支位置表、观测手段、退役判定 SQL（两张表 active bcrypt=0）、5 步删除清单（含 verifyTokenHash bcrypt 分流与计数器收尾）、本机实测记录。测试：token-hash.test.ts 4 单测（sha256 确定性、isBcryptHash 三前缀、verifyTokenHash 两路径）+ bcrypt-token.test.ts 3 集成（历史 bcrypt 令牌经 HTTP/WS 认证成功、metrics counters 增长，超时 30s 因全表扫描 O(N×100ms)）。真 PG 18.4 全量 vitest 219/219（23 文件）。增量项：dev 库 22 个 bcrypt 令牌轮换是运营动作（daemon 重签），脚本已给出判定与位置",
    "O19": "packages/server/Dockerfile 重写为多阶段（build: node:22-alpine + corepack pnpm@9 与 CI 一致 + frozen install + 编译 shared/server；runtime: pnpm deploy --prod 裁剪纯生产依赖 + 非 root slock 用户 + HEALTHCHECK wget 探测 /api/health 的 db:true + node dist/index.js，去掉旧的 pnpm dev/||true 容错写法）。关键坑：dist 在 .gitignore 中，pnpm deploy 包文件收集会跳过它 → Dockerfile 显式 cp 补拷（validate 脚本有对应断言防回归）。根 .dockerignore（node_modules/dist/.env/uploads/docs 等）。docker-compose.yml 生产化：postgres/valkey/server/minio 全 healthcheck、deploy.resources 资源限制、restart 策略、server 必选插值 ${JWT_SECRET:?}/${REFRESH_SECRET:?}（与 O5 应用层 exit(1) 双保险）、depends_on service_healthy、uploads-data 卷、去掉源码/依赖挂载、minio profile s3 保留。pnpm-workspace.yaml 开 injectWorkspacePackages=true（pnpm deploy 生产裁剪必需，pnpm9/10/11 兼容；lockfile 同步记录该 setting）。scripts/validate-compose.mjs（扫描 .pnpm 定位 yaml@* 传递依赖解析 compose，断言 healthcheck/资源限制/必选密钥/无源码挂载/uploads 卷/健康依赖/Dockerfile 多阶段非 root healthcheck deploy --prod dist 补拷/无 pnpm dev，接入 root package.json validate:compose + CI L1 job）。本机验证：pnpm 11 模拟 deploy 39.6MB（fastify/shared 就位、无 dev 依赖泄漏）；daemon 82/82 + server 219/219 + 全仓 typecheck 通过（inject 布局变更无回归）。增量项：本机无 docker CLI，真 docker build/compose up 未实跑（校验已入 CI，有 docker 的 CI runner 或机器上 docker compose up -d --build 验证 healthcheck 与容器非 root）"
  }
}
